# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code
# Personal, machine-specific settings. CLAUDE.md and TASKS.md are tracked
# deliberately — they are project documentation, not local config.
# Note: "*.local" above does NOT cover this, the filename ends in .json.
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
